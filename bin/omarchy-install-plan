#!/bin/bash

# omarchy:summary=Preview the ARM64 userspace compatibility plan without changing the system
# omarchy:args=[--target auto|rpi5|arm64] [--format text|json] [--pcie-gen 1|2 (Pi 5 only)]
# omarchy:examples=omarchy install plan --target rpi5 | omarchy install plan --target rpi5 --format json | omarchy install plan --target rpi5 --pcie-gen 2

set -euo pipefail

python3 "$OMARCHY_PATH/install/arm64/plan.py" "$@"
