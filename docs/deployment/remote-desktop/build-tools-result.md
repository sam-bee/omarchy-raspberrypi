# Pi RDP build-tool package result — 23 September 2026

**Result:** a reviewed local-archive pacman transaction added the 18 aarch64 build-tool packages listed in the [RDP runbook](README.md#preliminary-aarch64-build-dependency-preview). The Pi now has 347 packages: 35 explicit and 312 dependencies. No existing package was upgraded, removed, replaced, or reclassified. A pinned `hypr-rdp` source build and actual RDP client session remain separate, unproven stages.

## Recovery and package selection

The [unattended encrypted reboot](../unattended-boot/result.md) returned successfully before this transaction. Sierra's login password was subsequently rotated, so a new exact-state root-only baseline was captured at `/var/lib/omarchy-pi-deploy/20260923T084744Z` with the current 329 packages, account files, Sierra session and generated GPG files. Its encrypted off-Pi copy and checksum are private outside the repository and synced workspace; independent SHA-256, GPG decryption, outer archive listing, inner system archive listing and live Pi-side preservation checks passed before installation.

Fresh Arch Linux ARM metadata was refreshed only inside the root-owned scratch pacman database at `/var/tmp/omarchy-hypr-rdp-preview-20260923.K7iSxi`. The complete system-upgrade preview contained zero rows. The build-tool request resolved to exactly the 18 aarch64 packages and versions in the runbook's preliminary table, with five intended explicit roots (`rust`, `clang`, `cmake`, `make`, `pkgconf`) and 13 dependencies. The existing live pacman local and sync database hashes were unchanged by scratch resolution and download.

The scratch cache received exactly 18 archives and 18 detached signatures, with no partial files. Each archive's filename, compressed byte size and SHA-256 matched the refreshed sync metadata; each `.PKGINFO` name, version and architecture matched the frozen closure. Every signature verified with full trust against the copied Arch Linux ARM keyring under the Pi's `PackageRequired` and `PackageTrustedOnly` policy. The read-only `pacman -Up` preview selected exactly those 18 additions. Archive review found no `.INSTALL` scripts, no boot/kernel/initramfs, service, udev, sysusers/tmpfiles or package-hook payload, and only one new `/etc` path: `/etc/rhashrc` from `rhash`. `cmake` matched the installed MIME database hook.

## Transaction and preservation

Two independent SSH connections were open for the interactive transaction. Immediately before applying it, the old package versions/reasons and protected hashes matched the new baseline, `pacman -Dk` was clean, no database lock existed, and the local-archive preview still matched the frozen 18-row manifest. The final pacman prompt listed 18 packages and was accepted. `pacman -U --asdeps` installed the exact archives with normal dependency and signature checks; `pacman -D --asexplicit` then marked only the five requested build roots explicit. Pacman exited successfully and reported only the MIME database update and `ConditionNeedsUpdate` hooks.

Post-install inspection found the original 329 name/version pairs unchanged, exactly 18 new packages matching the manifest, 35 exact explicit roots and 312 exact dependency reasons, and a clean pacman database. All 2,429 protected hashes, 27 symlink targets and one absent path matched the fresh baseline. Boot ID, kernel, VT1, encrypted root and NVMe `/boot`, LUKS metadata, USB unlock key, EEPROM, boot files, enabled units/default target, firewall, addresses/routes, Sierra groups, and active/enabled SSH/networkd/Wi-Fi matched. A fresh, nonmultiplexed password SSH login passed. Avahi, seatd, Sierra's fumon and Foot server/socket remained inactive and disabled.

The independent `/etc` and `/boot` comparison found no missing old files, no mode change and no new `/boot` path. Reviewed effects were package-owned `/etc/rhashrc`, regenerated `/etc/ld.so.cache`, and six transient root GPG sockets. The pacman log appended exactly 18 installs and the two reported hooks, with no upgrade or removal. The signed archives remain in the root-owned scratch cache for this stage. Do not infer that the RDP server builds or works from this package result; those gates require a native build and an actual client view/input/reconnect test.
