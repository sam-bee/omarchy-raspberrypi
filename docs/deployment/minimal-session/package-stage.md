# Minimal UWSM and Quickshell package stage — 23 September 2026

**Status:** the reviewed 32-package transaction was installed on the Pi on 23 September 2026, after the post-smoke recovery backup `20260923T012942Z` and its encrypted off-Pi copy were verified. Package and preservation audits passed; no UWSM session has been staged or launched. The preceding Hyprland/Foot smoke is recorded in [the first-session result](../first-session/smoke-result.md). The procedure below remains the bounded recovery/reproduction record.

## Frozen package set

[The exact manifest](packages-2026-09-23.tsv) lists all 32 package names, versions, repositories, architectures, archive basenames, SHA-256 hashes and intended install reasons. The six explicit roots are `uwsm 0.27.0-1`, `quickshell 0.3.1-1`, `xdg-terminal-exec 0.14.3-2`, `inotify-tools 4.25.9.0-1`, `qt6-wayland 6.11.2-1` and `grim 1.5.0-2`. Grim adds only 19,256 bytes and permits an actual frame-capture check in a later session experiment. The other 26 packages are dependencies. The entire download is 40,606,088 bytes (38.72 MiB).

Resolution used a copy of the Pi's pacman database and trusted keyring in `/var/tmp/omarchy-pi-quattro-stage.rnIVBV`; the live sync database was not refreshed. The full-upgrade preview had **zero** rows. The native dependency preview and local `pacman -Up` preview both named these 32 new versions, with no installed-package upgrade, replacement or removal. All 32 archives and their 32 detached signatures were retained in that private Pi scratch cache and copied into the new root-only recovery backup. Pacman's download-only integrity check, detached signature verification, SHA-256 comparison and archive identity checks passed. Recheck the live package database and the exact cache/signature basename sets before any reproduction; drift needs renewed review. Do not refresh the live sync database to apply this frozen set.

## Installed result

The final interactive pacman prompt named exactly 32 additions and 260.28 MiB installed size, with no upgrade, reinstall, replacement or removal. `pacman -U --asdeps` exited successfully, the six roots were marked explicit, and `pacman -Dk` found no database errors. The five observed hooks created Avahi UID/GID 965, reloaded system and user managers, armed `ConditionNeedsUpdate`, and reloaded system bus configuration. Exact package and install-reason comparisons passed: 329 installed, 30 explicit, 299 dependencies; all 297 prior names, versions and reasons remain unchanged.

The postcheck passed 527 of 529 unchanged protected file hashes, all 25 protected symlinks, the one absent-path check and the USB unlock-key hash. The two changed protected files, `/etc/passwd` and `/etc/shadow`, gained only the reviewed Avahi line. `/etc/group` and `/etc/gshadow` also gained only that group; all existing account lines and Sierra's groups stayed unchanged. Their four `-` backup copies equal the corresponding pre-transaction live files byte-for-byte. Seven new `/etc` paths are owned by Avahi or tslib. `/etc/ld.so.cache` was regenerated, and six transient GnuPG agent sockets appeared under `/etc/pacman.d/gnupg`; no other `/etc` content changed. An independent recovery audit passed 28 checks and confirmed the same generated-file classification.

Boot path inventory, LUKS metadata, EEPROM, boot ID, enabled system units/default target, active SSH/networkd/Wi-Fi, addresses, routes and firewall rules matched the recovery baseline. Avahi's three system units and Sierra's `fumon.service` user unit remained inactive and disabled. A fresh, non-multiplexed SSH login succeeded after installation. The foreground VT was not switched.

## Archive and hook effects

The 32 archives contain 14,227 paths. They contain no `.INSTALL` scripts and no path in the protected boot, initramfs, encryption, SSH, Wi-Fi, networkd or firewall substrate. None matches the installed mkinitcpio or udev reload hooks. Existing hooks do match ordinary `ConditionNeedsUpdate`, system/user systemd daemon reloads, Avahi's D-Bus service/config reload, and Avahi's sysusers declaration. The Avahi system units are installed as files; no install script or matching hook enables or starts them. UWSM supplies `fumon.service` and `80-fumon.preset`, but the installed libalpm hooks/scripts contain no preset application. Verify these services remain disabled and inactive after the transaction rather than assuming package files alone determine state.

**Reviewed preservation exception:** `systemd-sysusers` will create the previously absent `avahi` system user and group. Expect additions to `/etc/passwd`, `/etc/shadow`, `/etc/group` and `/etc/gshadow`, including possible generated backup files. Review exact line diffs privately: only the new Avahi identity and group may be added; no existing identity, membership or Sierra groups may change. Protected-file SHA-256 checks must still pass for every other path. No package removal automatically removes a sysusers-created identity, so routine rollback preserves that account for separate review. Avahi's D-Bus reload hook may run; fresh SSH, routes, addresses and active network services are mandatory postchecks.

`libproxy`, `python-dbus` and `python-pyxdg` declare replacements/conflicts with older names, but those named packages are absent from the current Pi. A changed prestate that includes one of them is a stop condition. New package-owned `/etc/avahi` files and `/etc/ts.conf` are expected; compare `/etc` additions against the manifest. Do not enable Avahi or Fumon as part of this package stage.

## Transaction gate and bounded procedure

1. Record and independently verify a new backup of the **post-smoke** state, including package versions/reasons, account/group files, 529 protected hashes and symlinks/absent paths, `/boot` path inventory, LUKS metadata and unlock-key hash, EEPROM, enabled units/default target, boot ID, firewall rules, addresses, routes and active network/SSH services. Keep an encrypted, test-decryptable copy off the Pi. Keep two independent SSH connections throughout, one for recovery observation. Require the pre-transaction set of 297 packages (24 explicit, 273 dependencies), unchanged versions and no pacman lock. Keep the verified package cache and signatures in a root-only private Pi location with the new backup; do not put either backup or keyring in Git.
2. Compare the sorted archive basenames **and** `.sig` basenames with the manifest exactly, verify all manifest hashes and signatures with the Pi's existing trusted keyring, and run `pacman -Up --print-format '%n %v'` over exactly those 32 local archives. Reinspect installed hooks and archive metadata if package or hook state differs. A final interactive `pacman -U` prompt must list exactly 32 additions, no upgrades, reinstalls, removals or replacements; abort on any other transaction. Use local archives with `pacman -U --asdeps --` in the interactive PTY. Do not use `-Syu`, `--nodeps`, `--overwrite`, signature bypass or hook suppression. The udev sentinel from the prior Hyprland install is unnecessary for this exact closure because no udev rule or hook trigger path matches; if this finding changes, stop and review first.
3. After a successful transaction, mark **only** the six named roots explicit with `pacman -D --asexplicit`. Require `pacman -Dk` clean and exact installed set of 329 packages: all 297 old names and versions unchanged plus the 32 manifest additions, with 30 explicit and 299 dependency reasons. A failure or interrupted transaction still requires an actual-package inventory before further action; do not retry blindly.
4. Before staging any session file, compare all preservation evidence with the fresh backup. Review the Avahi account/group additions, their four pre-update backup copies, the package-owned `/etc` additions, the regenerated linker cache and transient GnuPG sockets as separate expected effects. Check the boot ID and foreground VT are unchanged, no new `/boot` paths, protected hashes/symlinks/absences, LUKS/USB-key/EEPROM values, enabled-unit set/default target, firewall rules, active SSH/networkd/Wi-Fi, addresses/routes and a **new independent SSH login**. Require `avahi-daemon.service`, `avahi-daemon.socket`, `avahi-dnsconfd.service` system units and Sierra's `fumon.service` user unit inactive/disabled. Any unexpected difference stops the later session experiment.

## Recovery boundary

For an unsuccessful package stage, preserve the pacman log and actual state first. Compare actual packages to the new prestate: refuse any missing old package, changed old version, or new name outside the 32-row manifest. Stop any later graphical session before package removal. If a rollback is then needed, remove only the **actual approved additions** together with ordinary `pacman -R --` dependency checks, without recursive orphan removal or dependency bypass. Review the removal prompt, then compare the old package versions and reasons exactly with the prestate and repeat the network/SSH and protected-state checks. Leave Avahi's generated account/group and caches for a separately reviewed cleanup; package removal alone does not restore a byte-identical `/etc`. Do not copy the saved pacman database over live state, unpack the whole recovery archive onto a running system, rebuild initramfs or reboot as routine rollback. Use the verified recovery bundle and physical/rescue access if remote access is lost.

## Session-runtime boundary

The draft minimal session at `install/arm64/session/` needs the six package roots above; its other startup commands (`git`, `tar`, `flock`, `python3`, `systemd-cat`, `dbus-update-activation-environment`, `hyprctl`, `foot`) are already installed. The minimal Workspaces/Clock bar does not enable AppLibrary app launching. `gtk-launch` is absent, so enabling that launcher later requires a separate GTK/runtime review. This package audit does not prove the draft UWSM/PAM session or Quickshell render path: test those only in a later, bounded session stage after the package preservation gate.
